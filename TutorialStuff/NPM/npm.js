// npm - global command, comes with node
// npm --version (--v)

// local dependency - used only in just one project
// npm i <packageName> - installs package in just one project

// global dependency - use it in all projects
// npm install -g <packageName> - installs package for all projects


// package.json - manifest file (stores important info about project/package)

// manual approach (create package.json in the root, create properties, etc.)
// npm init (command: step by step, press enter in order to skip)
// npm init -y (sets everything to default)